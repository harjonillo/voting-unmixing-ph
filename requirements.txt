# Runtime dependencies for the Streamlit app.
#
# Also the build-time deps for the static site: the deploy-static.yml workflow
# installs this file (plus python-fasthtml) to run scripts/build_static.py and
# scripts/build_site.py. `streamlit` is only needed to run app/streamlit_app.py
# directly (e.g. a server host); the static build does not import it.
streamlit
geopandas
pyarrow
plotly
matplotlib
seaborn
scikit-learn
scipy
tqdm
pandas
numpy
